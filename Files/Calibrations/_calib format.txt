// Straight from the code that reads the calib file:
// ITMLib::Objects::readRGBDCalib
// readIntrinsics(src, dest.intrinsics_rgb) // reads the following (for the rgb camera):
sizeX >> sizeY;
focalLength[0] >> focalLength[1];
centerPoint[0] >> centerPoint[1];

// readIntrinsics(src, dest.intrinsics_d) reads:
sizeX >> sizeY;
focalLength[0] >> focalLength[1];
centerPoint[0] >> centerPoint[1];

// readExtrinsics(src, dest.trafo_rgb_to_depth) reads:
Matrix4f calib;

// ITMLib::Objects::readDisparityCalib
[(kinect|affine)] // an optional word
(a, b) // two floats